---
description: Inline edit guardrails for SaaS Template frontend files.
alwaysApply: false
---
- Respect existing import paths; avoid mass rewrites.
- TanStack Router: colocate logic in route files; let codegen manage types.
- Keep react-hook-form patterns; validate with Zod when applicable.
- Extend Zustand stores in `src/stores` rather than adding new globals.
- Tailwind + shadcn styling: keep utility order; avoid inline styles unless necessary.
- Ensure new UI remains tree-shakeable and Vite-HMR friendly.
